#
# OMNeT++/OMNEST Makefile for mixim
#
# This file was generated with the command:
#  opp_makemake -f --nolink -O out -d power -d channelInfo -d decider -d coord -d blackboard -d basePhyLayer -d radioState -d connectionManager -d testUtils -d traci -d baseMobility -d nicTest -d mapping -I/usr/include/libxml2 -L../out/$(CONFIGNAME)/tests/power/utils -L../out/$(CONFIGNAME)/tests/testUtils -L../out/$(CONFIGNAME)/src/base -L../out/$(CONFIGNAME)/src/modules
#

# C++ include paths (with -I)
INCLUDE_PATH = -I/usr/include/libxml2 -I.

# Output directory
PROJECT_OUTPUT_DIR = ../out
PROJECTRELATIVE_PATH = tests
O = $(PROJECT_OUTPUT_DIR)/$(CONFIGNAME)/$(PROJECTRELATIVE_PATH)

# Object files for local .cc and .msg files
OBJS =

# Message files
MSGFILES =

#------------------------------------------------------------------------------

# Pull in OMNeT++ configuration (Makefile.inc or configuser.vc)

ifneq ("$(OMNETPP_CONFIGFILE)","")
CONFIGFILE = $(OMNETPP_CONFIGFILE)
else
ifneq ("$(OMNETPP_ROOT)","")
CONFIGFILE = $(OMNETPP_ROOT)/Makefile.inc
else
CONFIGFILE = $(shell opp_configfilepath)
endif
endif

ifeq ("$(wildcard $(CONFIGFILE))","")
$(error Config file '$(CONFIGFILE)' does not exist -- add the OMNeT++ bin directory to the path so that opp_configfilepath can be found, or set the OMNETPP_CONFIGFILE variable to point to Makefile.inc)
endif

include $(CONFIGFILE)

COPTS = $(CFLAGS)  $(INCLUDE_PATH) -I$(OMNETPP_INCL_DIR)
MSGCOPTS = $(INCLUDE_PATH)

# we want to recompile everything if COPTS changes,
# so we store COPTS into $COPTS_FILE and have object
# files depend on it (except when "make depend" was called)
COPTS_FILE = $O/.last-copts
ifneq ($(MAKECMDGOALS),depend)
ifneq ("$(COPTS)","$(shell cat $(COPTS_FILE) 2>/dev/null || echo '')")
$(shell $(MKPATH) "$O" && echo "$(COPTS)" >$(COPTS_FILE))
endif
endif

#------------------------------------------------------------------------------
# User-supplied makefile fragment(s)
# >>>
# inserted from file 'makefrag':
# makefrag for "tests" directory of MiXiM
#
# this file automatically gets inserted into Makefiles generated by opp_makemake

# preserve default target
all: $(TARGET)

baseMobility_dir basePhyLayer_dir blackboard_dir channelInfo_dir connectionManager_dir coord_dir mapping_dir radioState_dir decider_dir nicTest_dir power_dir traci_dir: testUtils_dir

.PHONY: testUtils

# <<<
#------------------------------------------------------------------------------

# Main target

all: $(OBJS) submakedirs Makefile
	@# Do nothing

submakedirs:  power_dir channelInfo_dir decider_dir coord_dir blackboard_dir basePhyLayer_dir radioState_dir connectionManager_dir testUtils_dir traci_dir baseMobility_dir nicTest_dir mapping_dir

.PHONY: all clean cleanall depend msgheaders  power channelInfo decider coord blackboard basePhyLayer radioState connectionManager testUtils traci baseMobility nicTest mapping
power: power_dir
channelInfo: channelInfo_dir
decider: decider_dir
coord: coord_dir
blackboard: blackboard_dir
basePhyLayer: basePhyLayer_dir
radioState: radioState_dir
connectionManager: connectionManager_dir
testUtils: testUtils_dir
traci: traci_dir
baseMobility: baseMobility_dir
nicTest: nicTest_dir
mapping: mapping_dir

power_dir:
	cd power && $(MAKE) all

channelInfo_dir:
	cd channelInfo && $(MAKE) all

decider_dir:
	cd decider && $(MAKE) all

coord_dir:
	cd coord && $(MAKE) all

blackboard_dir:
	cd blackboard && $(MAKE) all

basePhyLayer_dir:
	cd basePhyLayer && $(MAKE) all

radioState_dir:
	cd radioState && $(MAKE) all

connectionManager_dir:
	cd connectionManager && $(MAKE) all

testUtils_dir:
	cd testUtils && $(MAKE) all

traci_dir:
	cd traci && $(MAKE) all

baseMobility_dir:
	cd baseMobility && $(MAKE) all

nicTest_dir:
	cd nicTest && $(MAKE) all

mapping_dir:
	cd mapping && $(MAKE) all

.SUFFIXES: .cc

$O/%.o: %.cc $(COPTS_FILE)
	@$(MKPATH) $(dir $@)
	$(CXX) -c $(COPTS) -o $@ $<

%_m.cc %_m.h: %.msg
	$(MSGC) -s _m.cc $(MSGCOPTS) $?

msgheaders: $(MSGFILES:.msg=_m.h)
	cd power && $(MAKE) msgheaders
	cd channelInfo && $(MAKE) msgheaders
	cd decider && $(MAKE) msgheaders
	cd coord && $(MAKE) msgheaders
	cd blackboard && $(MAKE) msgheaders
	cd basePhyLayer && $(MAKE) msgheaders
	cd radioState && $(MAKE) msgheaders
	cd connectionManager && $(MAKE) msgheaders
	cd testUtils && $(MAKE) msgheaders
	cd traci && $(MAKE) msgheaders
	cd baseMobility && $(MAKE) msgheaders
	cd nicTest && $(MAKE) msgheaders
	cd mapping && $(MAKE) msgheaders

clean:
	-rm -rf $O
	-rm -f mixim mixim.exe libmixim.so libmixim.a libmixim.dll libmixim.dylib
	-rm -f ./*_m.cc ./*_m.h

	-cd power && $(MAKE) clean
	-cd channelInfo && $(MAKE) clean
	-cd decider && $(MAKE) clean
	-cd coord && $(MAKE) clean
	-cd blackboard && $(MAKE) clean
	-cd basePhyLayer && $(MAKE) clean
	-cd radioState && $(MAKE) clean
	-cd connectionManager && $(MAKE) clean
	-cd testUtils && $(MAKE) clean
	-cd traci && $(MAKE) clean
	-cd baseMobility && $(MAKE) clean
	-cd nicTest && $(MAKE) clean
	-cd mapping && $(MAKE) clean

cleanall: clean
	-rm -rf $(PROJECT_OUTPUT_DIR)

depend:
	$(MAKEDEPEND) $(INCLUDE_PATH) -f Makefile -P\$$O/ -- $(MSG_CC_FILES)  ./*.cc
	-cd power && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd channelInfo && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd decider && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd coord && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd blackboard && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd basePhyLayer && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd radioState && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd connectionManager && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd testUtils && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd traci && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd baseMobility && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd nicTest && if [ -f Makefile ]; then $(MAKE) depend; fi
	-cd mapping && if [ -f Makefile ]; then $(MAKE) depend; fi

# DO NOT DELETE THIS LINE -- make depend depends on it.

